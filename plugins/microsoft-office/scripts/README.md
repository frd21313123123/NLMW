# Helper Scripts

These scripts are optional helpers for the Microsoft Office plugin skills.

They are intentionally small and dependency-light:

- `word_docx_tool.py` uses `python-docx`.
- `excel_workbook_tool.py` uses `openpyxl`.

Run each script with `--help` for command details.
